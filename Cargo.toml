[package]
name = "kernel-mitigations-off"
version = "1.1.1"
edition = "2024"

[dependencies]
glob = "0.3.3"

[profile.release]
lto = "fat"       # thin or fat
codegen-units = 1
strip = true
opt-level = 3
